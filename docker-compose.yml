services:
  gateway:
    build:
      context: .
      target: gateway
    ports:
      - "8080:8080"
    depends_on:
      - backend
  backend:
    build:
      context: .
      target: backend
    ports:
      - "8081:8081"